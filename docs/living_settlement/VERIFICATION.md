# Журнал верификации и тестов (VERIFICATION.md)

Спецификация: `PlanetKI_Living_Settlement_TZ_v2.md`
Движок: Godot 4.7.2.stable.official.ed1daf0bf (`E:\Godot_v4.7.2-stable_win64.exe`)

## Baseline (S00)
- Базовый прогон тестов: `cmd /c "E:\Godot_v4.7.2-stable_win64.exe --headless --path . tests/test_runner.tscn"`
- Результат: 46/46 существующих тестов пройдены успешно (Stages A-F, 24 animals, combat stats, persistence).
- Зафиксированы легаси-проблемы:
  1. `GameManager.day_duration = 12.0` (вместо 900 сек).
  2. Сезонная логика и 4 сезона активны в коде.
  3. `WorldMapView._process` напрямую вызывает симуляцию граждан, фауны и перемещение армий.
  4. Старение населения происходит дискретно раз в "год" по календарю через `sim_yearly_aging` вместо непрерывного биографического возраста.
  5. Отсутствует `schema_version` и двухфазная загрузка в `SaveSystem`.

## S09: Рыбалка, группы охоты и восстановление
- Команда: `E:\Godot_v4.7.2-stable_win64.exe --headless --path . tests/test_runner.tscn`
- Результат: 82/82 тестов пройдены, `GODOT_EXIT=0`.
- Тест 79: береговой рыбный узел имеет конечный запас; рыбак ловит физический груз и зачисляет его только после доставки.
- Тест 80: группа содержит не более трёх охотников, а добыча делится из одного запаса туши без дублирования.
- Тест 81: незавершённый рост саженца переживает сериализацию и завершается после загрузки.
- Тест 82: запретную зону нельзя создать без активного закона; зона и закон сохраняются.

## S10: Экземпляры событий и журнал
- Команда: `E:\Godot_v4.7.2-stable_win64.exe --headless --path . tests/test_runner.tscn`
- Результат: 83/83 тестов пройдены, `GODOT_EXIT=0`.
- Тест 83: выбор разрешает только ожидающий экземпляр, повторный вызов не дублирует последствия, а выбранный вариант сохраняется после Save/Load.
- Ручная проверка загрузки скриптов: `--headless --editor --quit`, `GODOT_EXIT=0`.
