HEAD / ветка: 73420397fa37f3150a4c5ea9647550141b5d6806 / main
Текущий шаг: S10 — реальные экземпляры событий (3 вкладки)
Статус: in_progress

Готовое поведение S09:
- Береговые рыбные места: `MapResourceManager` создаёт доступные с суши узлы `fish` с запасом 18, восстановлением за 90 секунд и без подмены тайла карты при истощении.
- Рыбак: физически идёт к месту, ловит с задержкой, уменьшает конкретный запас, переносит пищевую партию `fish` и зачисляет её в экономику только при доставке на склад.
- Групповая охота: цель принимает от 1 до 3 охотников; список участников переносится на тушу. Каждый участник забирает свою часть одного конечного мясного запаса, без модального события.
- Восстановление леса: посаженный саженец продолжает расти после сохранения/загрузки и созревает в дерево с запасом 100 древесины.
- Запретные зоны: `SettlementData.set_reserved_zone()` отклоняет изменение без активного закона `land_territorial_zones`; состав зон и список законов фракции сохраняются.

Затронутые файлы:
- src/simulation/map_resource_manager.gd
- src/simulation/wildlife_manager.gd
- src/simulation/settlement.gd
- src/simulation/law_system.gd
- src/core/save_system.gd
- tests/test_runner.gd

Готовое поведение S10:
- `CivilizationEventManager` хранит независимые экземпляры с `instance_id`, состоянием, выбранным вариантом и датами; закрытие активного окна не удаляет запись из реестра.
- Журнал событий содержит вкладки «Решения», «Происшествия» и «Летопись», читающие один реестр; карточка здания больше не создаёт вымышленное событие.
- Игрок может отложить решение или не вмешиваться; последствия выбора применяются один раз и сохраняются после Save/Load.

Выполненные проверки:
- Headless прогон `tests/test_runner.tscn`: 83/83 тестов успешно пройдены, `GODOT_EXIT=0`.
- Тесты 79–82: рыбалка и доставка, группа 1–3 охотника с конечной тушей, стадийный рост дерева после сериализации, зоны только с законом и Save/Load.
- Тест 83: экземпляр события получает стабильный ID, не допускает повторного применения выбора и сохраняется после Save/Load.

Следующее конкретное действие:
- Завершить S10: добавить генератор реальных происшествий, дедлайны и привязку событий к существующим объектам, затем перейти к S11.
